"use strict";
function plus(a, b) {
    console.log(`${a} + ${b} = ${a + b}`);
    return a + b;
}
function minus(a, b) {
    console.log(`${a} - ${b} = ${a - b}`);
    return a - b;
}
function multiply(a, b) {
    console.log(`${a} * ${b} = ${a * b}`);
    return a * b;
}
